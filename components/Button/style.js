import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  btnContainer: {
    width: '100%',
    alignItems: 'center',
  },
  button: {
    width: '90%',
    padding: 20,
    borderWidth: 1,
    alignItems: 'center',
  },
});

export default style;
